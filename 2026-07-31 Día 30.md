recourse aws_ec2_instance

### Ejercicio 1 - Módulo 2 (60 ptos.)

**Contexto:** Tu equipo ha creado un modelo de Machine Learning que detecta spam. Para no pagar servidores encendidos 24/7, decidieron desplegarlo bajo un modelo Serverless (FaaS). Tu tarea es completar el script de Terraform (main.tf) que automatiza la creación de toda la infraestructura necesaria en AWS.

**Consigna:** Analiza el siguiente código e indica los espacios [ESPACIO X] con la opción técnica correcta de la lista que se encuentra debajo. Presta atención a qué componente hace cada bloque.

```

# 1. Crear el almacenamiento para guardar nuestro modelo de ML

resource **"[ESPACIO 1️⃣]"** "ml_artifacts" {

  bucket = "mi-bucket-de-ml-proyecto-2026" 

}

# 2. Crear las políticas de seguridad y permisos

resource **"[ESPACIO 2️⃣]"** "iam_for_lambda" {

  name = "iam_for_lambda"

  # ... (políticas de seguridad omitidas) ...

}

# 3. Definir la Función Serverless donde se ejecutará el código

resource **"[ESPACIO 3️⃣]"** "test_lambda" {

  function_name = "mi_funcion_predictora"

  role          = aws_iam_role.iam_for_lambda.arn

  handler       = "hola_mundo.lambda_handler" 

  # Archivo empaquetado que contiene el código

  filename      = **"[ESPACIO 4️⃣]"**

  # Entorno de ejecución de la función

  runtime       = **"[ESPACIO 5️⃣]"**

environment {

    variables = {

      BUCKET_DATOS = aws_s3_bucket.ml_artifacts.id

    }

  }

}

```  

##### Lista de opciones disponibles:  son 5

- aws_ec2_instance
    
- python3.9 (Espacio 5)
    
- aws_lambda_function (Espacio 3)
    
- ubuntu-latest
    
- aws_iam_role (espacio 2)
    
- lambda_function_payload.zip (espacio 4)
    
- aws_s3_bucket (espacio 1) 
    
- docker-compose.yml
    

  

---

Ejercicio 2 - Módulo 3 (40 ptos.)

Estás asistiendo a un desarrollador Junior que está intentando llevar una nueva aplicación web a producción. Durante el proceso, se encuentra con varios obstáculos operativos y te envía mensajes pidiendo ayuda para destrabarlos utilizando el ecosistema de herramientas de tu equipo.

**Consigna Principal:** Lee cada uno de los problemas planteados por el desarrollador y completa la frase con el **comando exacto** o la **herramienta correcta:**

1. **Empaquetado (Docker):** "Ya escribí mi archivo Dockerfile, pero no recuerdo cómo convertirlo en una imagen ejecutable en mi computadora. El comando exacto que debes usar en la terminal es: docker **[ESPACIO 1️⃣]** -t mi-app:v1 . "
    
2. **Infraestructura (Terraform):** "Escribí toda la red en mi archivo main.tf. Antes de crearla, quiero ver una 'vista previa' (el ticket de lo que se va a crear) para estar seguro de no cometer errores. El comando que debes usar es: terraform **[ESPACIO 2️⃣] "** (plan)
    
3. **Configuración (Ansible):** "Los servidores ya están creados, pero ahora necesito instalar Nginx en todos a la vez. No quiero instalar agentes en cada máquina, prefiero hacerlo mediante una conexión SSH directa (Push). La herramienta ideal para esta tarea es: **[ESPACIO 3️⃣] "** (ansible con el inventario.ini y un playbook.yml. Como el ejemplo de la clase 
   
	hosts: webservers
	tasks:
	   - name: Instalar Nginx 
	    apt:
	     name: nginx 
	     state: present
	   













---
2. build, init, 



ver clase 30 m3 clase 1 , clase3 y 4 de m2, clase 3 del m3 y clase 2 m3





---

# Grabación de la Clase

**Clase Grabada:** https://drive.google.com/file/d/1Ph5XmKpvVkGMKvMJjy7A-nPai72_WxlL/view
